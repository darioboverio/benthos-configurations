processor_resources:
  - label: cron_execution_logger
    log:
      level: INFO
      message: Organization ID = ${ORGANIZATION_ID} - cron execution with id = ${! meta("batch_id") } at ${! meta("fixed_timestamp") }

